/**
 * Custom hook for expense CRUD operations
 * Re-exports the useExpense hook from ExpenseContext for convenience
 */

export { useExpense } from '../contexts/ExpenseContext';
export default useExpense;
